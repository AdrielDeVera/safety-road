import { InquiryOptions } from './interfaces';
/**
 * This is an internal function (clients don't see this). Require `null` explicitly
 * to avoid bugs, where someone might add a field but forget to include it in callers.
 */
type ReplaceUndefinedWithNull<T> = T extends undefined ? null : T;
type ExplicitlyNull<T> = {
    [P in keyof T]-?: ReplaceUndefinedWithNull<T[P]>;
};
/**
 * Allows the Persona JS SDK iframe to receive events from the Persona web flow.
 *
 * You must call this method if building your own iframe.
 */
export declare function setupEvents(
/**
 * A unique string used for message passing between the JS SDK iframe and the Persona web app.
 */
containerId: string, { onLoad, onReady, onComplete, onEvent, onCancel, onError, templateId, templateVersionId, host, }: ExplicitlyNull<Pick<InquiryOptions, 'onLoad' | 'onReady' | 'onComplete' | 'onEvent' | 'onCancel' | 'onError' | 'templateId' | 'templateVersionId' | 'host'>>): () => void;
export {};
